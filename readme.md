# R-Timer
Simple rust app to create an alarm to help me learn rust

# How to build

Requires the [Rust](https://rust-lang.org/) compiler. 

CD into r-timer (yes, i was going to make it a timer but made an preset alarm instead)

then run

`` cargo build -- release ``

and then run the binary 